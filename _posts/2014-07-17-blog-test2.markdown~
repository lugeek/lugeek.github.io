---
layout: post
title: test code
categories: test
tags: test
---

##test code  
```java
    class playthread extends Thread{
    	public playthread(){
    		super("语音播放线程");
    	}
    	
    	byte [] buf=new byte[1024];
    	DatagramSocket socket=null;
    	DatagramPacket packet=null;
    	
    	public void run()
    	{
    		try
    		{		
    			socket=new DatagramSocket(20011);
    			packet=new DatagramPacket(buf,buf.length);
    		}
    		catch(Exception f)
    		{
    		}
    		while(Main_pinputu.flag)// 启动该线程后就不断接收消息 接收到一条就处理一条
    		{			
    			try
    			{			
    				socket.receive(packet);
    				Main_pinputu.audioTrack.write(packet.getData(), 0, packet.getData().length);//这是一条很长很长很长很长很长很长很长很长很长很长很长很长很长很长很长很长很长很长很长的注释！！！！！！！！！！！！！！！！
    				MySurfaceView.update(packet.getData());
    			}
    			catch(IOException e)
    			{
    				
    			}
    		}
    	}
    }
```